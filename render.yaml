services:
  - type: web
    name: cuidapp
    runtime: python
    plan: free
    buildCommand: bash build.sh
    startCommand: gunicorn cuidapp.wsgi:application --bind 0.0.0.0:$PORT --workers 3
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
